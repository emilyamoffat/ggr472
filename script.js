// Reference the button element in the DOM
const btn = document.getElementById("my-button");

// Attach a "click" event listener to the button
btn.addEventListener("click", () => {
    alert("You clicked me!");
});
